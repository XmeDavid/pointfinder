package com.prayer.pointfinder.entity;

/** OW-06: an open report waits for an admin; dismissed and removed are its two outcomes. */
public enum PublicationReportStatus {
    open,
    dismissed,
    removed
}
