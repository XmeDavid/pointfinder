{
  "name": "@pointfinder/game-core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure PointFinder domain logic: tag payloads, presence proofs, base status, offline queue. No platform code.",
  "type": "module",
  "types": "./src/index.ts",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@pointfinder/api": "workspace:*"
  }
}
