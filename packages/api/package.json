{
  "name": "@pointfinder/api",
  "version": "0.1.0",
  "private": true,
  "description": "Typed PointFinder API client, auth session, and realtime client. Platform-agnostic: bring your own fetch, token storage, and socket.",
  "type": "module",
  "types": "./src/index.ts",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  }
}
