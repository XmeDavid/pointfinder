
## HIGH

iPhone strings not rendering
Location permissions not syncing
Android check in, exit, appears checked in, mas is locked
iOS operator not syncing
Mapa on web is broken



## To review and merge
dev/todos branch with various old todos done + audio in rich text


## Flight findings
- [LOW] When logging in without internet iOS has an alert saying internet connection is not available, while android just shows red under the text box that pointfinder host was not reachable.

## New Features [Prio]
- [HIGH] Challange submission type: None - BACKEND + WEB DONE. Mobile apps still need UI changes: Android (PlayerGameplayScreens.kt) and iOS (SolveView.swift) need a "none" answer type path that auto-submits without showing text/file input. The operator mobile challenge edit screens also need the "none" option in the answer type picker.
- [HIGH] Send notifications to teams as operator from mobile.
- [HIGH] Pin dragging when setting the pin for a base is awful on web, not draggable, just clicking around and not very precise
- [MED] Show feedback to the teams after completing a challange, if feedback was added on the submission review.
- [MED] As admin pending invites stay forever, should be able to manually delete a pending invite, that link should then be invalid. We also show the operators list on top, so we also don't need the list of aproved invitations.
- [MED] The Overview and Dashboard tabs on the web, are quite pointless, they basically just show stats. The overview is important due to the go live button and checklist, but we can improve the rest. Maybe we show a base map, the checklist, base list, team list, all this things can be small
- [LOW] Individual mode (Where it's one player per team), for the user it is all just one thing. One join code for all?
- [LOW] Private matches, even with the code, you aprove who gets to join, a player joins and it shows the game locked(like when it has not started yet), until the operator aproves him.




## Code Quality / Tech Debt
- [LOW] Backend: N+1 query in PlayerService.getProgress() — assignment.getChallenge() lazy loads inside loop. Add JOIN FETCH to assignmentRepository.findByGameId().
- [LOW] Backend: JWT getTokenType defaults to "user" on missing claim — should throw instead of silently defaulting (JwtTokenProvider.java:91-95).
- [LOW] Backend: Broadcast code generation loop hardcoded to 10 attempts — works fine at current scale but should log attempts (GameService.java:324-336).
- [LOW] Frontend: WebSocket gameId stale closure on game switch — scheduleInvalidate RAF callback captures old gameId (useGameWebSocket.ts).
- [LOW] Frontend: AuthMedia blob URL leak on unmount during in-flight fetch — objectUrl is null in cleanup if fetch hasn't resolved (AuthMedia.tsx).
- [LOW] Frontend: SubmissionsPage blobCache ref grows unbounded — no eviction, holds references to revoked URLs (SubmissionsPage.tsx:76).

---

# Outside Codebase (manual / ops / launch)

## Localization Review
- Check German translation with a German expert.
- Review all strings by testers

## Launch / Release
- Publish on App Store.
- Publish on Play Store.

## Infra / Domain / Email Routing
- Set up reverse proxy or forwarding so `info@pointfinder.pt` goes to personal email.